server:
  port: 9090
  servlet:
    context-path: /admin
spring:
  application:
    name: deliver-admin
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 40MB

# 对称加密
aes:
  key: oszero
# 服务端发送接口地址
deliver-server:
  url: http://localhost:7070/open/sendMessage
