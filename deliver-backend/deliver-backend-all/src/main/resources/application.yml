server:
  port: 6060
  servlet:
    context-path: /backend
spring:
  application:
    name: deliver-all
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 40MB
# 对称加密
aes:
  key: oszero
# 服务端发送接口地址
deliver-server:
  url: /backend/open/sendMessage
